// filename: frontend/src/test/setup.ts
// Configuración inicial para tests de Vitest en Frontend

// Importa los matchers de jest-dom para que estén disponibles globalmente en 'expect'
import '@testing-library/jest-dom';

// Aquí podrías añadir otras configuraciones globales para tests en el futuro si fuera necesario
// (Ej: Limpiar localStorage antes de cada test, configurar mocks globales, etc.)
